package GroupExercises;

/*public class FighterTester {
    public static void main(String[] args) {
        Fighter tom = new Fighter();
        tom.setInfo("Tom", 10, 2);
        Fighter harry = new Fighter();
        harry.setInfo("Harry", 5, 4);
        System.out.println(declareWinner(tom, harry, tom.name) + " wins!");
    }

    private static String declareWinner(Fighter fighter1, Fighter fighter2, String name) {

        while (fighter1.health >= 0 && fighter2.health >= 0) {

            fighter2.health -= fighter1.damage; // tom attacks harry
            if (fighter2.health <= -1) return fighter1.name;
            fighter1.health -= fighter2.damage;//harry attacks tom
            if (fighter1.health <= -1) return fighter2.name;
        }
        return null;
    }

}*/
